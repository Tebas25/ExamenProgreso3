<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ExamenProgreso3.Views.PaginaConsulta"
             Title="PaginaConsulta">
    <VerticalStackLayout>
        <Label 
            Text="Welcome to .NET MAUI!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />

        <Entry Placeholder="Nombre del país" Text="{Binding NombrePais}" />
        <Button Text="Buscar" Command="{Binding BuscarPaisCommand}" />
        <Button Text="Limpiar" Command="{Binding LimpiarCommand}" />
        <Label Text="{Binding Pais.Name}" />
        <Label Text="{Binding Pais.Region}" />
        <Label Text="{Binding Pais.Map}" />

    </VerticalStackLayout>
</ContentPage>